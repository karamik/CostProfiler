name: TOTAL Release Pipeline

on:
  workflow_dispatch:
  release:
    types: [published, edited]

jobs:
  build-and-upload:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐹 Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: 🏗 Clean and Build
        run: |
          # Удаляем старые файлы конфигурации Go, если они есть
          rm -f go.mod go.sum
          
          # Инициализируем проект заново
          go mod init cost-profiler
          go mod tidy
          
          # Собираем конкретный файл в корне
          go build -o cost-profiler main.go

      - name: 🚀 Upload to Release
        uses: softprops/action-gh-release@v1
        with:
          files: cost-profiler
          tag_name: ${{ github.event.release.tag_name || 'v0.1.0-alpha' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
